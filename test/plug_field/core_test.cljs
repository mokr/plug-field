(ns plug-field.core-test
  (:require [cljs.test :refer-macros [is are deftest testing use-fixtures]]
            [plug-field.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
